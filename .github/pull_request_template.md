## Summary

Describe the focused change and the user-facing result.

## Validation

- [ ] `scripts\build-and-stage.cmd` completes on Windows.
- [ ] `node --check UIHost\ui\app.js` passes when JavaScript changed.
- [ ] Import, trigger release, and `ReleaseAll()` were tested when macro execution changed.
- [ ] UI changes were tested in dark and light themes.
- [ ] No `dist`, `.publish-temp`, `bridge`, local settings, logs, or private macros are included.

## Compatibility

- Registry or settings migration required:
- Existing imported macro packages affected:

## Screenshots

Required for visible UI changes.
